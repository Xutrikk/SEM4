<Application x:Class="lab4wpf5oop.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:lab4wpf5oop"
             StartupUri="LoginWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Ваши стили -->
            <Style x:Key="PlaceholderTextBoxStyle" TargetType="TextBox">
                <Setter Property="Foreground" Value="Gray"/>
                <Style.Triggers>
                    <Trigger Property="Text" Value="">
                        <Setter Property="Text" Value="{Binding Tag, RelativeSource={RelativeSource Self}}"/>
                    </Trigger>
                    <Trigger Property="IsKeyboardFocused" Value="True">
                        <Setter Property="Foreground" Value="Black"/>
                        <Setter Property="Text" Value="{Binding Text, RelativeSource={RelativeSource Self}, Mode=OneWay}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="PlaceholderPasswordBoxStyle" TargetType="PasswordBox">
                <Setter Property="Foreground" Value="Gray"/>
                <Style.Triggers>
                    <Trigger Property="Password" Value="">
                        <Setter Property="Tag" Value="{Binding Tag, RelativeSource={RelativeSource Self}}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- Merged dictionaries -->
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Languages/Strings.ru-RU.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>